{
  "name": "msg",
  "version": "1.0.0",
  "description": "A backend that listens to MongoDB changes and triggers Firebase notifications.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^11.10.0",
    "mongoose": "^7.2.0"
  }
}
